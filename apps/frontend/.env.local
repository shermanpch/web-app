# Backend API URL  
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000

# Set to 'true' to use Netlify proxy (requires redirects in netlify.toml)
# Set to 'false' or leave unset to use direct backend calls
NEXT_PUBLIC_USE_API_PROXY=false